\documentclass{article}
\usepackage{tkz-euclide}
\usepackage{amsmath}
\begin{document}
Sur la figure ci-dessous, toute les mesures sont en centimètres.\par\noindent
{\centering\begin{tikzpicture}[scale=2]
    \coordinate[label=left:A](A)at(0,0);
    \coordinate[label=below:B](B)at(-30:1);
    \coordinate[label=above:D](D)at(30:1);
    \coordinate[label=right:C](C)at(0:2);
    \draw(A)--(B)--(C)--(D)--cycle;
    \path(A)--(D)node[above left,midway]{$2x$};
    \path(D)--(C)node[above right,midway]{$3x+4$};
    \tkzMarkSegments[mark=s||](A,D A,B)
    \tkzMarkSegments[mark=s|](C,D C,B)
\end{tikzpicture}\par}
\begin{enumerate}
    \item Montrer que le périmètre de cette figure est égal à l'expression
    littérale $10x+8$.
    \item Calculer le périmètre de la figure pour $x=5$.
    \item Calculer le périmètre de la figure pour $x=1/3$.
    \item Pour quelle valeur de $x$ le périmètre vaut-il 28 cm ?
\end{enumerate}
% ---------------------
\vskip100pt
% ---------------------
\par\noindent
{\begin{tikzpicture}[rotate=-15]
    \coordinate(A)at(0,0);
    \coordinate(B)at(3,0);
    \coordinate(C)at(2,2);
    \draw node[below left]at(A){\strut A};
    \draw node[below right]at(B){\strut B};
    \draw node[above]at(C){\strut C};
    \path(A)--(B)node[midway, below]{9 cm};
    \path(B)--(C)node[midway, above right]{3,5 cm};
    \path(A)--(C)node[midway, above left]{4,5 cm};
    \draw(A)--(B)--(C)--cycle;
\end{tikzpicture}\hfil
\begin{tikzpicture}[rotate=20]
    \coordinate(A)at(0,0);
    \coordinate(B)at(3,0);
    \coordinate(C)at(2,2);
    \draw node[below left]at(A){\strut D};
    \draw node[below right]at(B){\strut E};
    \draw node[above]at(C){\strut F};
    \path(A)--(B)node[midway, below right]{0,8 dm};
    \path(B)--(C)node[midway, above right]{6 cm};
    \path(A)--(C)node[midway, above left]{4 cm};
    \draw(A)--(B)--(C)--cycle;
\end{tikzpicture}\hfil
\begin{tikzpicture}[rotate=110]
    \coordinate(A)at(0,0);
    \coordinate(B)at(3,0);
    \coordinate(C)at(2,2);
    \draw node[below right]at(A){\strut G};
    \draw node[above right]at(B){\strut H};
    \draw node[left]at(C){\strut J};
    \path(A)--(B)node[midway, above right]{3,7 cm};
    \path(B)--(C)node[midway, above left]{5,2 cm};
    \path(A)--(C)node[midway, below left]{10 cm};
    \draw(A)--(B)--(C)--cycle;
\end{tikzpicture}\par}
Les triangles ci-dessus ne sont pas en vrai grandeur. Pour chacun des trois triangles~:
\begin{enumerate}
    \item Dire si le triangle est constructible. Justifier par un calcul.
    \item S'il est constructible, le construire avec les outils de géométrie.
\end{enumerate}
% ---------------------
\vskip100pt
% ---------------------
\newdimen\vspaceCalculs
\vspaceCalculs=50pt% espace pour laisser l'élève écrire son calcul
\begin{enumerate}
    \item Calculer les expressions numériques suivantes en détaillant les étapes~:\medskip\par\noindent
        \hbox to.5\linewidth{$\text{A}=3+7\times5$\hfil}\hbox{$\text{B}=5-4+1$}\par\vskip\vspaceCalculs\noindent
        \hbox to.5\linewidth{$\text{C}=3\times(5-2\times(9-7))$\hfil}\hbox{$\text{D}=(3+11)(7-6)$}\vskip\vspaceCalculs\par
    \item Évaluer les deux expressions suivantes pour $x=0$ et $y=2$~:\medskip\par\noindent
    \hbox to.5\linewidth{$\text{E}=2(x+y)-xy$\hfil}\hbox{$\text{F}=1-3y+x$}\vskip\vspaceCalculs\par
    \item Réduire les deux expressions suivantes~:\medskip\par\noindent
    \hbox to.5\linewidth{$3x+2y+5x-y$\hfil}\hbox{$2\times3\times z-(4+3)z$}\par
\end{enumerate}

\end{document}
