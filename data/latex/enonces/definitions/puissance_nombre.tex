Soit $n$ un entier positif. Calculer un nombre à la \defined{puissance $n$},
c'est multiplier ce nombre par lui même $n$ fois.
$$a^n=\underbrace{a\times a\times...\times a}_{\text{n fois}}$$
Dans la notation $a^n$, $n$ se nomme \defined{exposant}.