Si deux droites sont parallèles à une même droite, alors elles sont parallèles entre elles.
\begin{center}
    Si $(d_1)\parallel(d_3)$ et $(d_2)\parallel(d_3)$ alors $(d_1)\parallel(d_2)$.
\end{center}